<%= form_for(dock) do |f| %>
  <% if dock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dock.errors.count, "error") %> prohibited this dock from being saved:</h2>

      <ul>
      <% dock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dock_number %>
    <%= f.number_field :dock_number %>
  </div>

  <div class="field">
    <%= f.label :available %>
    <%= f.text_field :available %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
